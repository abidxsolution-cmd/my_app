name: my_app_backend
description: Backend API for calculator website.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
